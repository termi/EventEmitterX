# Changelog at 20260314_200634

## Meta
- Model: ClaudeSonnet4.6
- Area: ci, docs, frontend
- Files affected: 8
- Directories touched: `.github/workflows/`, `docs-site/`, `docs-site/.vitepress/`, `demo/eventSignals-test-app/`

## Human-Readable Summary

### Documentation Site (GitHub Pages)
- New VitePress-based documentation site at `https://termi.github.io/EventEmitterX/`
- Landing page with project overview, feature cards, and links to API references
- **EventEmitterX** API reference page with full documentation from `docs/EventEmitterX.md`
- **EventSignal** API reference page with live demo link banner and full documentation from `docs/EventSignal.md`
- Built-in full-text search (local)
- GitHub link in navigation

### Live Demo
- `demo/eventSignals-test-app` is now built and deployed to `https://termi.github.io/EventEmitterX/demo/`
- Link to the demo is visible on the EventSignal page

### CI/CD
- New GitHub Actions workflow automatically deploys on every push to `main`
- Supports manual trigger (`workflow_dispatch`)

## Technical Details

### Files created/modified:
- **`.github/workflows/gh-pages.yml`** — GitHub Actions workflow: installs pnpm@6, builds root deps, builds demo with `VITE_BASE_URL=/EventEmitterX/demo/`, builds VitePress docs, merges outputs, deploys to GitHub Pages
- **`docs-site/package.json`** — VitePress site package with `"type": "module"` (required for ESM-only VitePress)
- **`docs-site/.vitepress/config.ts`** — VitePress config: `base: '/EventEmitterX/'`, nav/sidebar, search, social links, `ignoreDeadLinks: [/\/demo\//]` (demo is added post-build)
- **`docs-site/index.md`** — VitePress home layout with hero section and feature cards
- **`docs-site/eventemitterx.md`** — Includes content from `docs/EventEmitterX.md` via `<!--@include:-->`
- **`docs-site/eventsignal.md`** — Tip block with demo link + includes `docs/EventSignal.md`
- **`demo/eventSignals-test-app/vite.config.ts`** — Added `base: process.env.VITE_BASE_URL ?? '/'` to support configurable base path for GitHub Pages deployment
- **`.gitignore`** — Added `.vitepress/dist` and `.vitepress/cache` exclusions

### AI reasoning:
- Used VitePress `<!--@include:-->` feature to avoid content duplication — docs pages pull directly from `docs/*.md`
- `ignoreDeadLinks` is needed because `/demo/` directory is copied after VitePress build, so it doesn't exist during the build phase
- Root `pnpm install` must run before demo install so `link:../../` (`@termi/eventemitterx`) and `termi@polyfills` etc. are resolved correctly
- Used env var `VITE_BASE_URL` instead of `--base` CLI flag because pnpm@6 `run` command doesn't properly pass `--` arguments to underlying scripts

---

## [RU] Человеко-читаемая сумма изменений

### Сайт документации (GitHub Pages)
- Новый VitePress сайт документации доступен по адресу `https://termi.github.io/EventEmitterX/`
- Главная страница с обзором проекта, карточками возможностей и ссылками на API
- Страница **EventEmitterX** — полная документация API
- Страница **EventSignal** — баннер со ссылкой на живое демо + полная документация API
- Встроенный полнотекстовый поиск
- Ссылка на GitHub в навигации

### Живое демо
- `demo/eventSignals-test-app` собирается и деплоится на `https://termi.github.io/EventEmitterX/demo/`
- Ссылка на демо видна на странице EventSignal

### CI/CD
- Новый GitHub Actions workflow: автоматический деплой при каждом push в `main`
- Поддержка ручного запуска (`workflow_dispatch`)

