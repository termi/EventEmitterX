# Changelog at 20260314_222720

## Meta
- Model: ClaudeSonnet4.6
- Area: docs
- Files affected: 2
- Directories touched: `docs/`

## Human-Readable Summary

### Documentation — EventSignal.md / EventSignal_RU.md

Both `docs/EventSignal.md` and `docs/EventSignal_RU.md` received significant additions:

**Marketing intro section** (added before the API reference):
- New tagline/banner explaining why EventSignal stands out among reactive libraries
- Feature overview table with 9 categories (auto-tracking, React-native, async, event bridge, triggers, derived signals, promise/async, TypeScript, lifecycle)
- Quick Start code example covering: writable store, computed, async computed, React `use()` hook, direct JSX rendering
- "Reactive Composition" example — chained computed signals
- "Bridging External Events" example — `sourceEmitter` pattern

**Roadmap / Coming Soon section** (added at the end):
1. **Enhanced React Support** — visibility-aware rendering via `IntersectionObserver` + HTML signal bindings (`<EventSignal.$.input value={$} />`) with two-way auto-binding
2. **Signal Factory Helpers** — `createSignal`, `createComputedSignal`, `createReadonlySignal`, `createAsyncSignal`, `createSourceSignal`
3. **Standalone `@termi/eventsignal` package** — zero dependency on EventEmitterX
4. **Advanced Throttle & Debounce** — `ThrottleDescriptionDebounce` with throttle and debounce modes
5. **External Sync API** — `sync` option for localStorage, sessionStorage, custom async API
6. **And more** — `batch()`, `peek()`, DevTools integration, perf improvements

## Technical Details

- `docs/EventSignal.md`: Added ~220 lines of marketing content + roadmap section. Used `replace_string_in_file` twice: once for the intro block (after title, before Overview) and once for the roadmap (appended after Edge Cases section)
- `docs/EventSignal_RU.md`: Same structure, fully translated into Russian. Final file grew from 639 to ~858 lines

### AI reasoning:
- Marketing section placed before the API reference so readers immediately understand the value proposition before diving into technical details
- Kept existing API content 100% intact — only additions, no modifications
- Used `<!--@include:-->` pattern compatibility in mind: docs pages use the same files in VitePress, so no frontmatter added inside the files

---

## [RU] Человеко-читаемая сумма изменений

### Документация — EventSignal.md / EventSignal_RU.md

Оба файла `docs/EventSignal.md` и `docs/EventSignal_RU.md` получили значительные дополнения:

**Маркетинговый вводный раздел** (добавлен перед справочником API):
- Новый баннер/слоган объясняющий преимущества EventSignal
- Таблица возможностей с 9 категориями
- Пример Quick Start: хранилище, computed, async computed, React-хук `use()`, прямой JSX-рендеринг
- Пример "Реактивная композиция" — цепочки computed-сигналов
- Пример "Мост к внешним событиям" — паттерн `sourceEmitter`

**Раздел Roadmap / Скоро** (добавлен в конец):
1. Расширенная поддержка React — рендеринг с учётом видимости + биндинг на HTML-теги
2. Хелперы-фабрики сигналов — `createSignal`, `createComputedSignal` и др.
3. Отдельный пакет `@termi/eventsignal` без зависимости от EventEmitterX
4. Расширенный Throttle & Debounce с режимами throttle/debounce
5. Внешнее API синхронизации — localStorage, sessionStorage, пользовательское API
6. И многое другое — `batch()`, `peek()`, DevTools, производительность

